{
  "name": "11ty",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf ./_site/ && yarn --silent build:css && ELEVENTY_ENV=production CACHE_BUST=true eleventy",
    "build:css": "ELEVENTY_ENV=production eleventy --input ./styles.11ty.js",
    "start": "ELEVENTY_ENV=development eleventy --serve"
  },
  "browserslist": [
    "defaults"
  ],
  "devDependencies": {
    "@11ty/eleventy": "^0.9.0",
    "@mightyplow/eleventy-plugin-cache-buster": "^1.1.3"
  },
  "dependencies": {
    "autoprefixer": "^9.7.2",
    "cssnano": "^4.1.10",
    "node-sass": "^4.13.0",
    "postcss": "^7.0.23"
  }
}
